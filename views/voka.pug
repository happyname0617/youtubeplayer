doctype html
include head.pug
body
  include menu_login.pug
  .container-fluid.theme-showcase(role='main')
    .row
      .col-md-12.col-xs-12
        h2 Wörter A1
        div.voca
          each val, index in voca_A1
           if val.frequency > 0
            span.freqword.num1= val.displayForm
           else
            span.freqword= val.displayForm
        h2 Wörter A2
        div
          each val, index in voca_A2
           if val.frequency > 0
            span.freqword.num1= val.displayForm
           else
            span.freqword= val.displayForm
        h2 Wörter B1a
        div
          each val, index in voca_B1a
           if val.frequency > 0
            span.freqword.num1= val.displayForm
           else
            span.freqword= val.displayForm
        h2 Wörter B1b
        div
          each val, index in voca_B1b
           if val.frequency > 0
            span.freqword.num1= val.displayForm
           else
            span.freqword= val.displayForm
        h2 Wörter B2a
        div
          each val, index in voca_B2a
           if val.frequency > 0
            span.freqword.num1= val.displayForm
           else
            span.freqword= val.displayForm
        h2 Wörter B2b
        div
          each val, index in voca_B2b
           if val.frequency > 0
            span.freqword.num1= val.displayForm
           else
            span.freqword= val.displayForm
        h2 Wörter C1
        div
          each val, index in voca_C1
           if val.frequency > 0
            span.freqword.num1= val.displayForm
           else
            span.freqword= val.displayForm
        h2 Wörter C2
        div
          each val, index in voca_C2
           if val.frequency > 0
            span.freqword.num1= val.displayForm
           else
            span.freqword= val.displayForm
script.
  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
  })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');
  ga('create', 'UA-100905207-1', 'auto');
  ga('send', 'pageview');
